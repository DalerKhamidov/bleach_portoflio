export interface SkillCategory {
  title: string;
  body: string;
}

export interface WorkExperience {
  company: string;
  location: string;
  role: string;
  dates: string;
  bullets: string[];
}

export interface HumanMoment {
  title: string;
  description: string;
}
